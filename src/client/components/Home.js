import React from 'react';

const Home = () => (
  <div>
    <div>Home</div>
    <button
      onClick={() => console.log('hello')}
    >
      {'button'}
    </button>
  </div>
);

export default Home;
