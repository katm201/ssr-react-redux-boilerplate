/*
 *
 * HOME COMPONENT
 *
 */

import React from 'react';

const Home = () => (
  <div>
    <div>Home</div>
    <button
      onClick={() => console.log('hello Home')}
    >
      {'button'}
    </button>
  </div>
);

export default Home;
