import React from 'react';

const Test = () => (
  <div>
    <div>Test</div>
    <button
      onClick={() => console.log('hello Test')}
    >
      {'button'}
    </button>
  </div>
);

export default Test;
